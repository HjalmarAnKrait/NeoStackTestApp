<Window x:Class="NeoStackTestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NeoStackTestApp"
        mc:Ignorable="d"
        Padding="100"
        Title="MainWindow" Height="450" MinHeight="200" Width="800" MinWidth="200">
    
    <Grid ShowGridLines="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <ListBox x:Name="functionsListBox" DisplayMemberPath="FunctionName" Grid.Column="0" Background="Lavender">
            
        </ListBox>

        <StackPanel Grid.Column="1">
            <TextBlock Text="Выбранная функция: Линейная f(x,y) = ax + by'0 + c" x:Name="functionDescription"/>
            <TextBox x:Name="aTextBox" Text="kekk"/>
            <TextBox x:Name="bTextBox" Text="kek"/>
            <ComboBox x:Name="cComboBox" />
            <DataGrid x:Name="functionDataGrid">
              
            </DataGrid>
        </StackPanel>
        

    </Grid>
</Window>
